notes: Default settings for rvil_cdip configuration
ocr:
  backend: textract
  model_id: us.anthropic.claude-3-7-sonnet-20250219-v1:0
  system_prompt: >-
    You are an expert OCR system. Extract all text from the provided image
    accurately, preserving layout where possible.
  task_prompt: >-
    Extract all text from this document image. Preserve the layout, including
    paragraphs, tables, and formatting.
  features:
    - name: LAYOUT
  max_workers: 20
  image:
    target_width: null
    target_height: null
    dpi: 150
    preprocessing: null
classification:
  model: us.amazon.nova-2-lite-v1:0
  system_prompt: 'You are a multimodal document classification expert that analyzes
    business documents using both visual layout and textual content. Your task is
    to classify single-page documents into predefined categories based on their structural
    patterns, visual features, and text content. Your output must be valid JSON according
    to the requested format.

    <variables> <document-ocr-data>: OCR-extracted text content from the document
    page that provides textual information for classification <document-image>: Visual
    representation of the document page that provides layout, formatting, and visual
    structure information <document-types>: List of valid document types with their
    descriptions that the document must be classified into </variables>'
  task_prompt: "<task-description> Analyze the provided document using both its visual\
    \ layout and textual content to determine its document type and whether this page\
    \ begins a new document or continues the previous one. </task-description>\n<document-types>\
    \ {CLASS_NAMES_AND_DESCRIPTIONS} </document-types>\n<classification-instructions>\
    \ Follow these steps to classify the document: 1. Examine the visual layout: headers,\
    \ logos, formatting, structure, and visual organization 2. Analyze the textual\
    \ content: key phrases, terminology, purpose, and information type 3. Identify\
    \ distinctive features that match the document type descriptions 4. Decide if\
    \ this page starts a new document (output \"start\") or continues the previous\
    \ document (output \"continue\") 5. Consider both visual and textual evidence\
    \ together to determine the best match 6. CRITICAL: Only use document types explicitly\
    \ listed in the <document-types> section </classification-instructions>\n<output-format>\
    \ {\n  \"classification_reason\": \"Detailed reasoning including specific visual\
    \ and textual evidence that led to this classification\",\n  \"class\": \"exact_document_type_from_list\"\
    ,\n  \"document_boundary\": \"start or continue\"\n} </output-format>\n<<CACHEPOINT>>\n\
    <document-ocr-data> {DOCUMENT_TEXT} </document-ocr-data>\n<document-image> {DOCUMENT_IMAGE}\
    \ </document-image>\n<final-instructions> Analyze the document above by: 1. Applying\
    \ the <classification-instructions> to examine both visual and textual features\
    \ 2. Selecting ONLY from document types in <document-types> 3. Providing clear\
    \ reasoning with specific evidence 4. Outputting in the exact JSON format specified\
    \ in <output-format> </final-instructions>"
  temperature: 0
  top_p: 0
  top_k: 5
  max_tokens: 4096
  maxPagesForClassification: 0
  classificationMethod: multimodalPageLevelClassification
  sectionSplitting: llm_determined
  contextPagesCount: 0
  image:
    target_width: null
    target_height: null
    dpi: null
    preprocessing: null
extraction:
  model: us.amazon.nova-pro-v1:0
  system_prompt: >-
    You are a document assistant. Respond only with JSON. Never make up data,
    only provide data found in the document being provided.
  task_prompt: >-
    <background>

    You are an expert in document analysis and information extraction.  You can
    understand and extract key information from documents classified as type 

    {DOCUMENT_CLASS}.

    </background>


    <task>

    Your task is to take the unstructured text provided and convert it into a
    well-organized table format using JSON. Identify the main entities,
    attributes, or categories mentioned in the attributes list below and use
    them as keys in the JSON object.  Then, extract the relevant information
    from the text and populate the corresponding values in the JSON object.

    </task>


    <extraction-guidelines>

    Guidelines:
        1. Ensure that the data is accurately represented and properly formatted within
        the JSON structure
        2. Include double quotes around all keys and values
        3. Do not make up data - only extract information explicitly found in the
        document
        4. Do not use /n for new lines, use a space instead
        5. If a field is not found or if unsure, return null
        6. All dates should be in MM/DD/YYYY format
        7. Do not perform calculations or summations unless totals are explicitly given
        8. If an alias is not found in the document, return null
        9. Guidelines for checkboxes:
         9.A. CAREFULLY examine each checkbox, radio button, and selection field:
            - Look for marks like ✓, ✗, x, filled circles (●), darkened areas, or handwritten checks indicating selection
            - For checkboxes and multi-select fields, ONLY INCLUDE options that show clear visual evidence of selection
            - DO NOT list options that have no visible selection mark
         9.B. For ambiguous or overlapping tick marks:
            - If a mark overlaps between two or more checkboxes, determine which option contains the majority of the mark
            - Consider a checkbox selected if the mark is primarily inside the check box or over the option text
            - When a mark touches multiple options, analyze which option was most likely intended based on position and density. For handwritten checks, the mark typically flows from the selected checkbox outward.
            - Carefully analyze visual cues and contextual hints. Think from a human perspective, anticipate natural tendencies, and apply thoughtful reasoning to make the best possible judgment.
        10. Think step by step first and then answer.

    </extraction-guidelines>

    If the attributes section below contains a list of attribute names and
    descriptions, then output only those attributes, using the provided
    descriptions as guidance for finding the correct values. 

    <attributes>

    {ATTRIBUTE_NAMES_AND_DESCRIPTIONS}

    </attributes>


    <<CACHEPOINT>>


    <document-text>

    {DOCUMENT_TEXT}

    </document-text>


    <document_image>

    {DOCUMENT_IMAGE}

    </document_image>


    <final-instructions>

    Extract key information from the document and return a JSON object with the
    following key steps: 1. Carefully analyze the document text to identify the
    requested attributes 2. Extract only information explicitly found in the
    document - never make up data 3. Format all dates as MM/DD/YYYY and replace
    newlines with spaces 4. For checkboxes, only include options with clear
    visual selection marks 5. Use null for any fields not found in the document
    6. Ensure the output is properly formatted JSON with quoted keys and values
    7. Think step by step before finalizing your answer

    </final-instructions>
  temperature: 0
  top_p: 0
  top_k: 5
  max_tokens: 10000
  image:
    target_width: null
    target_height: null
    dpi: null
    preprocessing: null
  agentic:
    enabled: false
    review_agent: false
    review_agent_model: us.anthropic.claude-haiku-4-5-20251001-v1:0
  custom_prompt_lambda_arn: null
assessment:
  hitl_enabled: false
  default_confidence_threshold: '0.8'
  enabled: true
  model: us.amazon.nova-lite-v1:0
  system_prompt: >-
    You are a document analysis assessment expert. Your role is to evaluate the
    confidence and accuracy of data extraction results by analyzing them against
    source documents.

    Provide accurate confidence scores for each assessment. When bounding boxes
    are requested, provide precise coordinate locations where information
    appears in the document.
  task_prompt: >-
    <background> You are an expert document analysis assessment system. Your
    task is to evaluate the confidence of extraction results for a document of
    class {DOCUMENT_CLASS} and provide precise spatial localization for each
    field. </background>

    <task> Analyze the extraction results against the source document and
    provide confidence assessments AND bounding box coordinates for each
    extracted attribute. Consider factors such as: 1. Text clarity and OCR
    quality in the source regions  2. Alignment between extracted values and
    document content  3. Presence of clear evidence supporting the extraction 
    4. Potential ambiguity or uncertainty in the source material  5.
    Completeness and accuracy of the extracted information 6. Precise spatial
    location of each field in the document </task>

    <assessment-guidelines> For each attribute, provide:  - A confidence score
    between 0.0 and 1.0 where:
       - 1.0 = Very high confidence, clear and unambiguous evidence
       - 0.8-0.9 = High confidence, strong evidence with minor uncertainty
       - 0.6-0.7 = Medium confidence, reasonable evidence but some ambiguity
       - 0.4-0.5 = Low confidence, weak or unclear evidence
       - 0.0-0.3 = Very low confidence, little to no supporting evidence
    - A clear explanation of the confidence reasoning - Precise spatial
    coordinates where the field appears in the document

    Guidelines:  - Base assessments on actual document content and OCR quality 
    - Consider both text-based evidence and visual/layout clues  - Account for
    OCR confidence scores when provided  - Be objective and specific in
    reasoning  - If an extraction appears incorrect, score accordingly with
    explanation - Provide tight, accurate bounding boxes around the actual text
    </assessment-guidelines>

    <spatial-localization-guidelines> For each field, provide bounding box
    coordinates: - bbox: [x1, y1, x2, y2] coordinates in normalized 0-1000 scale
    - page: Page number where the field appears (starting from 1)

    Coordinate system: - Use normalized scale 0-1000 for both x and y axes - x1,
    y1 = top-left corner of bounding box   - x2, y2 = bottom-right corner of
    bounding box - Ensure x2 > x1 and y2 > y1 - Make bounding boxes tight around
    the actual text content - If a field spans multiple lines, create a bounding
    box that encompasses all relevant text </spatial-localization-guidelines>

    <final-instructions> Analyze the extraction results against the source
    document and provide confidence assessments with spatial localization.
    Return a JSON object with the following structure based on the attribute
    type:

    For SIMPLE attributes:  {
      "simple_attribute_name": {
        "confidence": 0.85,
        "bbox": [100, 200, 300, 250],
        "page": 1
      }
    }

    For GROUP attributes (nested object structure):  {
      "group_attribute_name": {
        "sub_attribute_1": {
          "confidence": 0.90,
          "bbox": [150, 300, 250, 320],
          "page": 1
        },
        "sub_attribute_2": {
          "confidence": 0.75,
          "bbox": [150, 325, 280, 345],
          "page": 1
        }
      }
    }

    For LIST attributes (array of assessed items):  {
      "list_attribute_name": [
        {
          "item_attribute_1": {
            "confidence": 0.95,
            "bbox": [100, 400, 200, 420],
            "page": 1
          },
          "item_attribute_2": {
            "confidence": 0.88,
            "bbox": [250, 400, 350, 420],
            "page": 1
          }
        },
        {
          "item_attribute_1": {
            "confidence": 0.92,
            "bbox": [100, 425, 200, 445],
            "page": 1
          },
          "item_attribute_2": {
            "confidence": 0.70,
            "bbox": [250, 425, 350, 445],
            "page": 1
          }
        }
      ]
    }

    IMPORTANT:   - For LIST attributes like "Transactions", assess EACH
    individual item in the list separately with individual bounding boxes - Each
    transaction should be assessed as a separate object in the array with its
    own spatial coordinates - Do NOT provide aggregate assessments for list
    items - assess each one individually with precise locations - Include
    assessments AND bounding boxes for ALL attributes present in the extraction
    results - Match the exact structure of the extracted data - Provide page
    numbers for all bounding boxes (starting from 1) </final-instructions>

    <<CACHEPOINT>>

    <document-image> {DOCUMENT_IMAGE} </document-image>

    <ocr-text-confidence-results> {OCR_TEXT_CONFIDENCE}
    </ocr-text-confidence-results>

    <<CACHEPOINT>>

    <attributes-definitions> {ATTRIBUTE_NAMES_AND_DESCRIPTIONS}
    </attributes-definitions>

    <extraction-results> {EXTRACTION_RESULTS} </extraction-results>
  temperature: 0
  top_p: 0
  top_k: 5
  max_tokens: 10000
  validation_enabled: false
  image:
    target_width: null
    target_height: null
    dpi: null
    preprocessing: null
  granular:
    enabled: true
    list_batch_size: 1
    simple_batch_size: 3
    max_workers: 20
summarization:
  enabled: true
  model: us.anthropic.claude-3-7-sonnet-20250219-v1:0
  system_prompt: >-
    You are a document summarization expert who can analyze and summarize
    documents from various domains including medical, financial, legal, and
    general business documents. Your task is to create a summary that captures
    the key information, main points, and important details from the document.
    Your output must be in valid JSON format. \nSummarization Style:
    Balanced\\nCreate a balanced summary that provides a moderate level of
    detail. Include the main points and key supporting information, while
    maintaining the document's overall structure. Aim for a comprehensive yet
    concise summary.\n Your output MUST be in valid JSON format with markdown
    content. You MUST strictly adhere to the output format specified in the
    instructions.
  task_prompt: >-
    <document-text>

    {DOCUMENT_TEXT}

    </document-text>

    Analyze the provided document (<document-text>) and create a comprehensive
    summary.

    CRITICAL INSTRUCTION: You MUST return your response as valid JSON with the
    EXACT structure shown at the end of these instructions. Do not include any
    explanations, notes, or text outside of the JSON structure.

    Create a summary that captures the essential information from the document.
    Your summary should:

    1. Extract key information, main points, and important details

    2. Maintain the original document's organizational structure where
    appropriate

    3. Preserve important facts, figures, dates, and entities

    4. Reduce the length while retaining all critical information

    5. Use markdown formatting for better readability (headings, lists,
    emphasis, etc.)

    6. Cite all relevant facts from the source document using inline citations
    in the format [Cite-X, Page-Y] where X is a sequential citation number and Y
    is the page number

    7. Format citations as markdown links that reference the full citation list
    at the bottom of the summary
      Example: [[Cite-1, Page-3]](#cite-1-page-3)

    8. At the end of the summary, include a "References" section that lists all
    citations with their exact text from the source document in the format:
      [Cite-X, Page-Y]: Exact text from the document

    Output Format:

    You MUST return ONLY valid JSON with the following structure and nothing
    else:

    ```json {
      "summary": "A comprehensive summary in markdown format with inline citations linked to a references section at the bottom"
    } ```

    Do not include any text, explanations, or notes outside of this JSON
    structure. The JSON must be properly formatted and parseable.
  temperature: 0
  top_p: 0
  top_k: 5
  max_tokens: 4096
criteria_validation:
  model: us.anthropic.claude-3-5-sonnet-20240620-v1:0
  system_prompt: ''
  task_prompt: ''
  temperature: 0
  top_p: 0.01
  top_k: 20
  max_tokens: 4096
  semaphore: 3
  max_chunk_size: 180000
  token_size: 4
  overlap_percentage: 10
  response_prefix: <response>
agents:
  error_analyzer:
    model_id: us.anthropic.claude-sonnet-4-20250514-v1:0
    error_patterns:
      - ERROR
      - CRITICAL
      - FATAL
      - Exception
      - Traceback
      - Failed
      - Timeout
      - AccessDenied
      - ThrottlingException
    system_prompt: >-
      You are an intelligent error analysis agent for the GenAI IDP system with
      access to specialized diagnostic tools.


      GENERAL TROUBLESHOOTING WORKFLOW:

      1. Identify document status from DynamoDB

      2. Find any errors reported during Step Function execution

      3. Collect relevant logs from CloudWatch

      4. Identify any performance issues from X-Ray traces

      5. Provide root cause analysis based on the collected information


      TOOL SELECTION STRATEGY:

      - If user provides a filename: Use cloudwatch_document_logs and
      dynamodb_status for document-specific analysis

      - For system-wide issues: Use cloudwatch_logs and dynamodb_query

      - For execution context: Use lambda_lookup or stepfunction_details

      - For distributed tracing: Use xray_trace or xray_performance_analysis


      ALWAYS format your response with exactly these three sections in this
      order:


      ## Root Cause

      Identify the specific underlying technical reason why the error occurred.
      Focus on the primary cause, not symptoms.


      ## Recommendations

      Provide specific, actionable steps to resolve the issue. Limit to top
      three recommendations only.


      <details>

      <summary><strong>Evidence</strong></summary>


      Format evidence with source information. Include relevant data from tool
      responses:


      **For CloudWatch logs:**

      **Log Group:** [full log_group name]

      **Log Stream:** [full log_stream name]

      ```

      [ERROR] timestamp message

      ```


      **For other sources (DynamoDB, Step Functions, X-Ray):**

      **Source:** [service name and resource]

      ```

      Relevant data from tool response

      ```


      </details>


      FORMATTING RULES:

      - Use the exact three-section structure above

      - Make Evidence section collapsible using HTML details tags

      - Include relevant data from all tool responses (CloudWatch, DynamoDB,
      Step Functions, X-Ray)

      - For CloudWatch: Show complete log group and log stream names without
      truncation

      - Present evidence data in code blocks with appropriate source labels
        
      ANALYSIS GUIDELINES:

      - Use multiple tools for comprehensive analysis when needed

      - Start with document-specific tools for targeted queries

      - Use system-wide tools for pattern analysis

      - Combine DynamoDB status with CloudWatch logs for complete picture

      - Leverage X-Ray for distributed system issues


      ROOT CAUSE DETERMINATION:

      1. Document Status: Check dynamodb_status first

      2. Execution Details: Use stepfunction_details for workflow failures

      3. Log Analysis: Use cloudwatch_document_logs or cloudwatch_logs for error
      details

      4. Distributed Tracing: Use xray_performance_analysis for service
      interaction issues

      5. Context: Use lambda_lookup for execution environment


      RECOMMENDATION GUIDELINES:

      For code-related issues or system bugs:

      - Do not suggest code modifications

      - Include error details, timestamps, and context


      For configuration-related issues:

      - Direct users to UI configuration panel

      - Specify exact configuration section and parameter names


      For operational issues:

      - Provide immediate troubleshooting steps

      - Include preventive measures


      TIME RANGE PARSING:

      - recent: 1 hour

      - last week: 168 hours  

      - last day: 24 hours

      - No time specified: 24 hours (default)


      IMPORTANT: Do not include any search quality reflections, search quality
      scores, or meta-analysis sections in your response. Only provide the three
      required sections: Root Cause, Recommendations, and Evidence.
    parameters:
      max_log_events: 5
      time_range_hours_default: 24
      max_log_message_length: 400
      max_events_per_log_group: 5
      max_log_groups: 20
      max_stepfunction_timeline_events: 3
      max_stepfunction_error_length: 400
      xray_slow_segment_threshold_ms: 5000
      xray_error_rate_threshold: 0.05
      xray_response_time_threshold_ms: 10000
  chat_companion:
    model_id: us.anthropic.claude-haiku-4-5-20251001-v1:0
    error_patterns:
      - ERROR
      - CRITICAL
      - FATAL
      - Exception
      - Traceback
      - Failed
      - Timeout
      - AccessDenied
      - ThrottlingException
    system_prompt: |2-

                  You are an intelligent error analysis agent for the GenAI IDP system with access to specialized diagnostic tools.

                    GENERAL TROUBLESHOOTING WORKFLOW:
                    1. Identify document status from DynamoDB
                        2. Find any errors reported during Step Function execution
                    3. Collect relevant logs from CloudWatch
                    4. Identify any performance issues from X-Ray traces
                5. Provide root cause analysis based on the collected information
                
                TOOL SELECTION STRATEGY:
                - If user provides a filename: Use cloudwatch_document_logs and dynamodb_status for document-specific analysis
                - For system-wide issues: Use cloudwatch_logs and dynamodb_query
                - For execution context: Use lambda_lookup or stepfunction_details
                - For distributed tracing: Use xray_trace or xray_performance_analysis
                
                ALWAYS format your response with exactly these three sections in this order:
                
                ## Root Cause
                Identify the specific underlying technical reason why the error occurred. Focus on the primary cause, not symptoms.

                ## Recommendations
                    Provide specific, actionable steps to resolve the issue. Limit to top three recommendations only.

                <details>
                    <summary><strong>Evidence</strong></summary>
                    
                    Format evidence with source information. Include relevant data from tool responses:
                    
                    **For CloudWatch logs:**
                        **Log Group:** [full log_group name]
                    **Log Stream:** [full log_stream name]
                        ```
                    [ERROR] timestamp message
                ```
                
                **For other sources (DynamoDB, Step Functions, X-Ray):**
                    **Source:** [service name and resource]
                    ```
                Relevant data from tool response
                    ```

                </details>

                    FORMATTING RULES:
                - Use the exact three-section structure above
                - Make Evidence section collapsible using HTML details tags
                - Include relevant data from all tool responses (CloudWatch, DynamoDB, Step Functions, X-Ray)
                - For CloudWatch: Show complete log group and log stream names without truncation
                - Present evidence data in code blocks with appropriate source labels
                      
                    ANALYSIS GUIDELINES:
                - Use multiple tools for comprehensive analysis when needed
                    - Start with document-specific tools for targeted queries
                    - Use system-wide tools for pattern analysis
                    - Combine DynamoDB status with CloudWatch logs for complete picture
                    - Leverage X-Ray for distributed system issues
                        
                        ROOT CAUSE DETERMINATION:
                        1. Document Status: Check dynamodb_status first
                    2. Execution Details: Use stepfunction_details for workflow failures
                    3. Log Analysis: Use cloudwatch_document_logs or cloudwatch_logs for error details
                    4. Distributed Tracing: Use xray_performance_analysis for service interaction issues
                    5. Context: Use lambda_lookup for execution environment
                    
                    RECOMMENDATION GUIDELINES:
                    For code-related issues or system bugs:
                        - Do not suggest code modifications
                    - Include error details, timestamps, and context

                    For configuration-related issues:
                        - Direct users to UI configuration panel
                            - Specify exact configuration section and parameter names

                            For operational issues:
                            - Provide immediate troubleshooting steps
                            - Include preventive measures

                            TIME RANGE PARSING:
                            - recent: 1 hour
                    - last week: 168 hours  
                            - last day: 24 hours
                            - No time specified: 24 hours (default)
                    
                    IMPORTANT: Do not include any search quality reflections, search quality scores, or meta-analysis sections in your response. Only provide the three required sections: Root Cause, Recommendations, and Evidence.
    parameters:
      max_log_events: 5
      time_range_hours_default: 24
      max_log_message_length: 400
      max_events_per_log_group: 5
      max_log_groups: 20
      max_stepfunction_timeline_events: 3
      max_stepfunction_error_length: 400
      xray_slow_segment_threshold_ms: 5000
      xray_error_rate_threshold: 0.05
      xray_response_time_threshold_ms: 10000
classes:
  - description: >-
      A formal written correspondence with sender/recipient addresses, date,
      salutation, body, and closing signature
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: letter
    properties:
      Date:
        type: string
        description: >-
          The date when the letter was written. Look for a standalone date or
          text following phrases like 'written on' or 'dated'.
      Cc:
        type: string
        description: >-
          Names of people who receive a copy of the letter in addition to the
          main recipient. Often preceded by 'cc', 'carbon copy', or 'copy to'.
      SenderAddress:
        type: string
        description: >-
          The physical address of the sender, typically appearing at the top of
          the letter. May be labeled as 'address', 'location', or 'from
          address'.
        x-aws-idp-confidence-threshold: '0.8'
      Signature:
        type: string
        description: >-
          The handwritten name or mark of the sender at the end of the letter.
          May follow terms like 'signed by' or simply appear at the bottom of
          the document.
      Subject:
        type: string
        description: >-
          The topic or main point of the letter. Often preceded by 'subject',
          'RE:', or 'regarding'.
      SenderName:
        type: string
        description: >-
          The name of the person or entity who wrote or sent the letter. Look
          for text following or near terms like 'from', 'sender', 'authored by',
          'written by', or at the end of the letter before a signature.
        x-aws-idp-confidence-threshold: '0.85'
      RecipientAddress:
        type: string
        description: >-
          The physical address where the letter is to be delivered. Often
          labeled as 'to address' or 'delivery address', typically appearing
          below the recipient name.
      RecipientName:
        type: string
        description: >-
          The name of the person or entity receiving the letter. Look for this
          after 'to', 'recipient', 'addressee', or at the beginning of the
          letter.
        x-aws-idp-confidence-threshold: '0.9'
      LetterType:
        type: string
        description: >-
          The category or classification of the letter, such as 'complaint',
          'inquiry', 'invitation', etc. May be indicated by 'type' or
          'category'.
      ReferenceNumber:
        type: string
        description: >-
          An identifying number or code associated with the letter. Look for
          labels like 'ref', 'reference', or 'our ref'.
    $id: letter
  - description: >-
      A structured document with labeled fields, checkboxes, or blanks requiring
      user input and completion
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: form
    properties:
      SubmissionDate:
        type: string
        description: >-
          The date when the form was submitted or filed. Look for text near
          'date', 'submitted on', or 'filed on'.
      Comments:
        type: string
        description: >-
          Additional notes or remarks about the form. Look for sections labeled
          'notes', 'remarks', or 'comments'.
      ProcessedBy:
        type: string
        description: >-
          The name of the person or department that processed the form. May be
          indicated by 'processor', 'handled by', or 'approved by'.
      ApprovalStatus:
        type: string
        description: >-
          The current state of approval for the form, such as 'approved',
          'pending', 'rejected', etc. Look for terms like 'status', 'approved',
          or 'pending'.
      FormId:
        type: string
        description: >-
          The unique identifier for the form, typically a number or alphanumeric
          code. Often labeled as 'form number', 'id', or 'reference number'.
      SubmitterId:
        type: string
        description: >-
          An identification number for the person submitting the form, such as
          social security number, employee ID, etc. Often labeled as 'id
          number', 'identification', or 'reference'.
      SubmitterName:
        type: string
        description: >-
          The name of the person who submitted the form. May be labeled as
          'name', 'submitted by', or 'filed by'.
      ProcessingDate:
        type: string
        description: >-
          The date when the form was processed or completed. Look for labels
          like 'processed on' or 'completion date'.
      FormType:
        type: string
        description: >-
          The category or purpose of the form, such as 'application',
          'registration', 'request', etc. May be identified by 'form name',
          'document type', or 'form category'.
      Department:
        type: string
        description: >-
          The organizational unit responsible for the form. Often abbreviated as
          'dept' or may appear as 'department' or 'division'.
    $id: form
  - description: >-
      A billing document listing items/services, quantities, prices, payment
      terms, and transaction totals
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: invoice
    properties:
      CustomerAddress:
        type: string
        description: >-
          The address where the invoice is sent or goods are delivered. May be
          labeled as 'billing address' or 'ship to'.
      DueDate:
        type: string
        description: >-
          The deadline by which payment must be made. Look for 'due date',
          'payment due', or 'payable by'.
      Quantities:
        type: string
        description: >-
          The number of each item provided. Often abbreviated as 'qty' or may
          appear as 'quantity' or 'amount' in a table column.
      VendorName:
        type: string
        description: >-
          The name of the business providing goods or services. May be labeled
          as 'vendor', 'seller', 'supplier', or simply appear prominently at the
          top of the invoice.
      Tax:
        type: string
        description: >-
          The amount of tax charged on the invoice. May be labeled as 'tax',
          'VAT', or 'GST', usually appearing after the subtotal.
      PaymentTerms:
        type: string
        description: >-
          The conditions under which payment should be made, such as '30 days',
          'COD', etc. Often labeled as 'terms', 'payment terms', or
          'conditions'.
      VendorAddress:
        type: string
        description: >-
          The physical location of the vendor. Look for 'address', 'location',
          or 'business address', typically near the vendor name.
      InvoiceDate:
        type: string
        description: >-
          The date when the invoice was issued. May be labeled as 'date',
          'invoice date', or 'billing date'.
      UnitPrices:
        type: string
        description: >-
          The cost per unit of each item. May be labeled as 'price', 'rate', or
          'unit cost'.
      PoNumber:
        type: string
        description: >-
          The purchase order reference number. May be abbreviated as 'PO' or
          appear as 'purchase order' or 'order reference'.
      TotalAmount:
        type: string
        description: >-
          The final amount to be paid including all charges. Look for 'total',
          'grand total', or 'amount due', typically the last figure on the
          invoice.
      Subtotal:
        type: string
        description: >-
          The sum of all items before tax and other charges. Look for 'subtotal'
          or 'net amount', typically found toward the bottom of the invoice.
      CustomerName:
        type: string
        description: >-
          The name of the person or entity being billed. Often preceded by
          'customer', 'buyer', or 'bill to'.
      InvoiceNumber:
        type: string
        description: The unique identifier for the invoice. Look for 'invoice no', 'invoice
      Items:
        type: string
        description: >-
          Descriptions of the products or services provided. Look for a section
          with 'description', 'item details', or 'products', usually in a table
          format.
    $id: invoice
  - description: >-
      A professional summary showcasing work experience, education, skills, and
      achievements for job applications
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: resume
    properties:
      Skills:
        type: string
        description: >-
          The abilities and competencies of the applicant. Look for a section
          titled 'skills', 'competencies', or 'expertise'.
      FullName:
        type: string
        description: >-
          The complete name of the job applicant, typically appearing
          prominently at the top of the resume. May be simply labeled as 'name'
          or 'applicant name'.
      Education:
        type: string
        description: >-
          The academic history and qualifications of the applicant. Look for a
          section with 'education', 'academic background', or 'qualifications'.
      Achievements:
        type: string
        description: >-
          Notable accomplishments and recognition. May be labeled as
          'achievements', 'accomplishments', or 'awards'.
      Languages:
        type: string
        description: >-
          Languages known and level of proficiency. Often appears in a section
          labeled 'languages' or 'language proficiency'.
      References:
        type: string
        description: >-
          People who can vouch for the applicant's abilities. Look for
          'references' or 'referees', typically at the end of the resume.
      ContactInfo:
        type: string
        description: >-
          The phone number, email, and address of the applicant. Look for a
          section with 'contact', 'phone', 'email', or 'address', usually near
          the top of the resume.
      Experience:
        type: string
        description: >-
          The work history and previous roles of the applicant. Often labeled as
          'experience', 'work history', or 'employment'.
      Certifications:
        type: string
        description: >-
          Professional credentials and qualifications. May be labeled as
          'certifications', 'certificates', or 'credentials'.
      Objective:
        type: string
        description: >-
          A statement outlining the applicant's career goals. May be labeled as
          'objective', 'summary', or 'profile', typically appearing early in the
          resume.
    $id: resume
  - description: >-
      A peer-reviewed academic document with abstract, methodology, results,
      citations, and research findings
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: scientific_publication
    properties:
      Funding:
        type: string
        description: >-
          Financial support received for the research. May be indicated by
          'funding', 'grants', or 'financial support'.
      Keywords:
        type: string
        description: >-
          Terms that represent the core topics of the paper. Look for a list
          labeled 'keywords' or 'key terms', typically after the abstract.
      Issue:
        type: string
        description: >-
          The issue number of the journal. May be labeled as 'issue' or
          abbreviated as 'no'.
      JournalName:
        type: string
        description: >-
          The name of the journal where the paper was published. Look for text
          following 'journal' or 'publication'.
      Abstract:
        type: string
        description: >-
          A brief summary of the paper's content. Often labeled as 'abstract' or
          'summary', appearing before the main text.
      Title:
        type: string
        description: >-
          The name of the scientific paper, typically appearing prominently at
          the beginning. May be labeled as 'title', 'paper title', or 'article
          title'.
      Volume:
        type: string
        description: >-
          The volume number of the journal. Often abbreviated as 'vol' or may
          appear as 'volume'.
      Institutions:
        type: string
        description: >-
          The organizations with which the authors are affiliated. May be
          labeled as 'affiliations' or 'institutions'.
      Pages:
        type: string
        description: >-
          The page range of the paper in the journal. Often abbreviated as 'pp'
          or may appear as 'pages'.
      CorrespondingAuthor:
        type: string
        description: >-
          The author responsible for communication regarding the paper. Look for
          'corresponding author' or 'contact author'.
      PublicationDate:
        type: string
        description: >-
          The date when the paper was published. May be preceded by 'published'
          or labeled as 'publication date'.
      Authors:
        type: string
        description: >-
          The researchers who conducted the study and wrote the paper. Look for
          names after 'authors', 'contributors', or 'researchers', usually
          following the title.
      Doi:
        type: string
        description: >-
          The Digital Object Identifier for the paper, a unique alphanumeric
          string. Look for 'DOI' or 'digital object identifier'.
    $id: scientific_publication
  - description: >-
      An internal business communication with TO/FROM/DATE/SUBJECT headers for
      organizational announcements or directives
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: memo
    properties:
      MemoType:
        type: string
        description: >-
          The category or classification of the memo, such as 'informational',
          'directive', etc. May be indicated by 'type' or 'category'.
      Subject:
        type: string
        description: >-
          The topic of the memo. Often preceded by 'subject', 'RE:', or
          'regarding'.
      MemoDate:
        type: string
        description: >-
          The date when the memo was written. Look for 'date' or 'memo date',
          typically near the top of the document.
      From:
        type: string
        description: >-
          The person or department that wrote the memo. May be labeled as
          'from', 'sender', or 'author'.
      ActionRequired:
        type: string
        description: >-
          Steps that should be taken in response to the memo. Look for 'action',
          'response needed', or 'next steps'.
      To:
        type: string
        description: >-
          The intended recipient of the memo. Look for text after 'to',
          'recipient', or 'addressee'.
      Priority:
        type: string
        description: >-
          The urgency level of the memo, such as 'urgent', 'high', 'normal',
          etc. Look for 'priority' or 'urgency'.
      DistributionList:
        type: string
        description: >-
          Additional people who receive copies of the memo. May be labeled as
          'distribution', 'cc', or 'copy'.
      Department:
        type: string
        description: >-
          The organizational unit issuing the memo. Often abbreviated as 'dept'
          or may appear as 'department' or 'division'.
      ReferenceNumber:
        type: string
        description: >-
          An identifying number or code for the memo. Look for 'reference' or
          'ref no'.
    $id: memo
  - description: >-
      A promotional material featuring product images, marketing text,
      calls-to-action, and branding elements
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: advertisement
    properties:
      Features:
        type: string
        description: >-
          Notable qualities or benefits of the product. Often listed under
          'features', 'benefits', or 'highlights'.
      CallToAction:
        type: string
        description: >-
          What the advertisement encourages the reader to do. Often appears as
          imperative phrases like 'call now', 'visit today', or 'order now'.
      Price:
        type: string
        description: >-
          The cost of the product or service. Look for currency symbols or
          numbers followed by terms like 'price', 'cost', or 'special offer'.
      TermsConditions:
        type: string
        description: >-
          Legal constraints or limitations of the offer. Look for fine print
          labeled as 'terms', 'conditions', or 'restrictions'.
      ValidityPeriod:
        type: string
        description: >-
          The timeframe during which the offer is valid. Look for phrases like
          'valid until', 'offer ends', or 'expires'.
      PromotionDetails:
        type: string
        description: >-
          Information about special deals or discounts. May be introduced with
          'promotion', 'offer', or 'deal'.
      ContactInfo:
        type: string
        description: >-
          How to reach the advertiser. May include phone numbers, websites, or
          addresses following 'contact', 'call', or 'visit'.
      ProductName:
        type: string
        description: >-
          The name of the item or service being advertised. Look for prominently
          displayed text that could be a 'product', 'item', or 'service' name.
      Brand:
        type: string
        description: >-
          The company or manufacturer of the product. May be indicated by a logo
          or text labeled as 'brand', 'company', or 'manufacturer'.
      Disclaimer:
        type: string
        description: >-
          Legal statements limiting liability or making clarifications. Usually
          appears as fine print introduced by 'disclaimer' or phrases like
          'terms apply' or 'conditions apply'.
    $id: advertisement
  - description: >-
      A digital message with email headers (To/From/Subject), timestamps, and
      conversational threading
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: email
    properties:
      CcAddress:
        type: string
        description: >-
          Email addresses of additional recipients who receive copies. Look for
          'cc' or 'carbon copy' followed by one or more email addresses.
      ThreadId:
        type: string
        description: >-
          An identifier for the email conversation. May be labeled as 'thread'
          or 'conversation', typically not visible to regular users.
      DateSent:
        type: string
        description: >-
          The date and time when the email was sent. Look for 'date', 'sent on',
          or 'received', typically in the email header.
      Attachments:
        type: string
        description: >-
          Files included with the email. May be indicated by 'attached',
          'attachment', or 'enclosed', often with icons or file names.
      BccAddress:
        type: string
        description: >-
          Email addresses of hidden recipients. May be labeled as 'bcc' or
          'blind copy'.
      Subject:
        type: string
        description: >-
          The topic of the email. Often preceded by 'subject', 'RE:', or
          'regarding'.
      ToAddress:
        type: string
        description: >-
          The email address of the primary recipient. May be labeled as 'to',
          'recipient', or 'sent to'.
      MessageId:
        type: string
        description: >-
          A unique identifier for the specific email. Look for 'message id' or
          'email id', usually hidden in the email metadata.
      Priority:
        type: string
        description: >-
          The urgency level of the email, such as 'high', 'normal', etc. Look
          for 'priority' or 'importance'.
      FromAddress:
        type: string
        description: >-
          The email address of the sender. Look for text following 'from',
          'sender', or 'sent by', typically at the beginning of the email
          header.
    $id: email
  - description: >-
      A survey instrument containing numbered questions with multiple choice,
      rating scales, or open-ended responses
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: questionnaire
    properties:
      Instructions:
        type: string
        description: >-
          Guidance on how to complete the questionnaire. Look for text
          introduced by 'instructions', 'directions', or 'guidelines'.
      RequestHandlingSatisfactionRating:
        type: string
        description: >-
          Measures the customer's level of satisfaction with the way their
          request was handled. Checkbox selection on the satisfaction scale with
          options like "Very Satisfied", "Somewhat Satisfied" etc.
      FuturePurchaseIntent:
        type: string
        description: >-
          A measure of whether the user will continue to buy the product they
          contacted about. Checkbox selection with options like "I definitely
          would", "I probably would" etc.
      ProductRecommendationIntent:
        type: string
        description: >-
          A measure of whether the caller is willing to recommend the product
          that they called about to others. Checkbox selection with options like
          "I definitely would", "I probably would" etc.
      FormTitle:
        type: string
        description: >-
          The name or title of the questionnaire. Look for prominently displayed
          text at the beginning that could be a 'title', 'survey name', or
          'questionnaire name'.
      SubmissionDate:
        type: string
        description: >-
          The date when the questionnaire was completed. Look for 'date',
          'completed on', or 'submitted'.
      RequiredFields:
        type: string
        description: >-
          Questions that must be answered to complete the questionnaire. Often
          marked with an asterisk (*) or explicitly labeled as 'required',
          'mandatory', or 'must answer'.
      SurveyId:
        type: string
        description: >-
          A unique identifier for the questionnaire. May be labeled as 'survey
          id', 'reference number', or 'form id'.
      OverallSatisfactionRating:
        type: string
        description: >-
          This rating BEST describes the way the customer feels about the
          representative's response to their request for asssistance. Checkbox
          selection on the satisfaction scale with options like "I was very
          satisfied", "I was somewhat satisfied" etc.
      SectionHeaders:
        type: string
        description: >-
          Titles for different segments of the questionnaire. Often appear as
          bold or larger text introducing a new 'section', 'part', or 'segment'.
      RespondentInfo:
        type: string
        description: >-
          Information about the person completing the questionnaire. May include
          fields labeled 'respondent', 'participant', or 'name'.
      CompletionStatus:
        type: string
        description: >-
          Whether the questionnaire has been fully completed. Look for
          indicators of 'status', 'completion', or 'progress', often shown as a
          percentage or progress bar.
      PhoneCallRepresentativeKnowledgeRating:
        type: string
        description: >-
          Measures the perceived knowledge level of the customer service
          representative during the phone interaction. Checkbox selection on the
          satisfaction scale with options like "Very Satisfied", "Somewhat
          Satisfied" etc.
      ResponseOptions:
        type: string
        description: >-
          Possible answers for multiple-choice questions. Look for checkboxes,
          radio buttons, or dropdown menus with 'options', 'choices', or
          'answers'.
      QuestionTypes:
        type: string
        description: >-
          The format of questions (multiple choice, free text, etc.). May be
          indicated by 'type', 'question format', or 'response format'.
      PhoneCallRepresentativeCourtesy:
        type: string
        description: >-
          Measures the perceived politeness and professionalism of the customer
          service representative during the phone interaction. Checkbox
          selection on the satisfaction scale with options like "Very
          Satisfied", "Somewhat Satisfied" etc.
        x-aws-idp-evaluation-method: ''
    $id: questionnaire
  - description: >-
      A technical document detailing precise requirements, measurements,
      standards, and implementation criteria
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: specification
    properties:
      Standards:
        type: string
        description: >-
          Industry guidelines or certifications met. Look for references to
          'standards', 'certifications', or 'compliance'.
      ApprovalInfo:
        type: string
        description: >-
          Details about who has validated the specification. May be indicated by
          phrases like 'approved by', 'certified by', or 'validated'.
      Requirements:
        type: string
        description: >-
          Necessary conditions or resources. May be introduced with
          'requirements', 'prerequisites', or 'needed'.
      Materials:
        type: string
        description: >-
          What the product is made from. May be indicated by 'materials',
          'composition', or phrases like 'made from'.
      RevisionHistory:
        type: string
        description: >-
          Record of changes to the specification. Often labeled as 'revisions',
          'changes', or 'updates', typically in a table format.
      ProductName:
        type: string
        description: >-
          The name of the item being specified. Look for text labeled as
          'product', 'item', or 'model', typically appearing prominently at the
          beginning.
      Version:
        type: string
        description: >-
          The iteration or release number. May be indicated by 'version',
          'revision', or 'release', often followed by a number or code.
      Compatibility:
        type: string
        description: >-
          What the product can work with. Look for text following 'compatible
          with', 'works with', or 'supports'.
      TechnicalDetails:
        type: string
        description: >-
          Specific characteristics and capabilities. Look for sections labeled
          'specifications', 'tech specs', or 'details', often presented in a
          detailed list.
      Dimensions:
        type: string
        description: >-
          Physical measurements of the product. Often labeled as 'dimensions',
          'size', or 'measurements', usually including length, width, height,
          etc.
    $id: specification
  - description: >-
      An unstructured document lacking distinctive formatting or
      purpose-specific elements of other categories
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: generic
    properties:
      Language:
        type: string
        description: The dominant language used in the document text
      Title:
        type: string
        description: The title of the document, if known - otherwise 'unknown'
      Author:
        type: string
        description: The author or authors of the document, if known - otherwise 'unknown'
      Summary:
        type: string
        description: A 1-paragraph summary of the contents of the document
      Metadata:
        type: string
        description: Additional metadata about the document, if any
    $id: generic
  - $schema: https://json-schema.org/draft/2020-12/schema
    $defs:
      RevenueDetails:
        type: object
        description: Details of different types of revenue.
        properties:
          Grants:
            type: string
            description: Details of grants.
          Treatment-Revenue-Bentonville:
            type: string
            description: Treatment revenue for Bentonville.
          Treatment-Revenue-Tontitown:
            type: string
            description: Treatment revenue for Tontitown.
          Reserve-Fund-Revenue:
            type: string
            description: Reserve fund revenue details.
      AdditionalDetails:
        type: object
        description: Additional details and notes.
        properties:
          Notes:
            type: string
            description: Additional notes and explanations.
      ExpenseDetails:
        type: object
        description: Details of different types of expenses.
        properties:
          Office:
            type: string
            description: Office expenses.
          Property-Taxes:
            type: string
            description: Property taxes expenses.
          Telephone:
            type: string
            description: Telephone expenses.
          Accounting:
            type: string
            description: Accounting expenses.
          Design-Fund-Fees-&-Permits:
            type: string
            description: Design fund fees and permits expenses.
          Legal-Fees:
            type: string
            description: Legal fees expenses.
          Automobile-Allowance:
            type: string
            description: Automobile allowance expenses.
          Grant-Expense:
            type: string
            description: Grant expense details.
          Public-Notices:
            type: string
            description: Public notices expenses.
          Design-Fund-Interest-Expense:
            type: string
            description: Design fund interest expense.
          Trustee-Expense:
            type: string
            description: Trustee expense details.
          Convenance-Line-Interest-Exp:
            type: string
            description: Convenance line interest expense.
          Marketing:
            type: string
            description: Marketing expenses.
          Meeting-Expense:
            type: string
            description: Meeting expense details.
          Other-Expense:
            type: string
            description: Other miscellaneous expenses.
          Engineering:
            type: string
            description: Engineering expenses.
          Postage:
            type: string
            description: Postage expenses.
          Interest-Expense:
            type: string
            description: Interest expense details.
          Professional-Fees:
            type: string
            description: Professional fees expenses.
          Payroll-Expenses:
            type: string
            description: Payroll expenses details.
          Health-Insurance:
            type: string
            description: Health insurance expenses.
          Bond-Financing-Cost:
            type: string
            description: Bond financing cost.
          Travel:
            type: string
            description: Travel expenses.
          Equipment:
            type: string
            description: Equipment expenses.
          Insurance:
            type: string
            description: Insurance expenses.
      SummaryInformation:
        type: object
        description: Summary information of the budget report.
        properties:
          2011-Actual:
            type: string
            description: Actual expenditures for the year 2011.
          Total-Annual-Budget:
            type: string
            description: Total annual budget.
          Convenance-D/S:
            type: string
            description: Convenance details.
          Monthly-Invoice:
            type: string
            description: Monthly invoice details.
          Operating:
            type: string
            description: Operating details.
          2011-Budget:
            type: string
            description: Total budget for the year 2011.
          2012-Budget:
            type: string
            description: Budget for the year 2012.
          Facility-D/S:
            type: string
            description: Facility details.
    description: >-
      A detailed budget report, including budget allocations, actuals, and
      notes.
    type: object
    x-aws-idp-document-type: budget
    properties:
      RevenueDetails:
        description: Details of different types of revenue.
        $ref: '#/$defs/RevenueDetails'
      AdditionalDetails:
        description: Additional details and notes.
        $ref: '#/$defs/AdditionalDetails'
      DocumentClass:
        type: string
        description: Short name based on document content.
      ExpenseDetails:
        description: Details of different types of expenses.
        $ref: '#/$defs/ExpenseDetails'
      DocumentDescription:
        type: string
        description: Description about the document in less than 50 words.
      SummaryInformation:
        description: Summary information of the budget report.
        $ref: '#/$defs/SummaryInformation'
    $id: budget
  - description: Any document written primarily in a non-English language
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    x-aws-idp-document-type: language
    properties:
      Language:
        type: string
        description: The dominant language used in the document text
      Title:
        type: string
        description: The title of the document, if known - otherwise 'unknown'
      Author:
        type: string
        description: The author or authors of the document, if known - otherwise 'unknown'
      Summary:
        type: string
        description: A 1-paragraph summary of the contents of the document
      Metadata:
        type: string
        description: Additional metadata about the document, if any
    $id: language
  - $schema: https://json-schema.org/draft/2020-12/schema
    $defs:
      Article-Content:
        type: object
        description: Content of the news article.
        properties:
          Event-Description:
            type: string
            description: Description of the news event.
          Introduction:
            type: string
            description: Introduction paragraph of the article.
          Summary:
            type: string
            description: One paragraph summary of the article.
      Article-Metadata:
        type: object
        description: Metadata about the news article.
        properties:
          Author:
            type: string
            description: Author of the article.
          Publisher-Name:
            type: string
            description: Name of the newspaper or news source publishing the article.
          Publication-Date:
            type: string
            description: Date of publication.
          Article-Title:
            type: string
            description: Title of the article.
    description: A newspaper article or news bulletin
    type: object
    x-aws-idp-document-type: news_article
    properties:
      Article-Content:
        description: Content of the news article.
        $ref: '#/$defs/Article-Content'
      Article-Metadata:
        description: Metadata about the news article.
        $ref: '#/$defs/Article-Metadata'
    $id: news_article
  - $schema: https://json-schema.org/draft/2020-12/schema
    description: >-
      A handwritten document, journal, memo, doodle, notes, or any other image
      with hand writing.
    type: object
    x-aws-idp-document-type: handwritten
    properties:
      Language:
        type: string
        description: The dominant language used in the document text
      Title:
        type: string
        description: The title of the document, if known - otherwise 'unknown'
      Author:
        type: string
        description: The author or authors of the document, if known - otherwise 'unknown'
      Summary:
        type: string
        description: A 1-paragraph summary of the contents of the document
      Metadata:
        type: string
        description: Additional metadata about the document, if any
    $id: handwritten
discovery:
  without_ground_truth:
    model_id: us.amazon.nova-pro-v1:0
    system_prompt: >-
      You are an expert in processing forms. Extracting data from images and
      documents. Analyze forms line by line to identify field names, data types,
      and organizational structure. Focus on creating comprehensive blueprints
      for document processing without extracting actual values.
    temperature: 0
    top_p: 0
    max_tokens: 10000
    user_prompt: >-
      This image contains forms data. Analyze the form line by line. Image may
      contains multiple pages, process all the pages.  Form may contain multiple
      name value pair in one line.  Extract all the names in the form including
      the name value pair which doesn't have value.  Organize them into groups,
      extract field_name, data_type and field description Field_name should be
      less than 60 characters, should not have space use '-' instead of space.
      field_description is a brief description of the field and the location of
      the field like box number or line number in the form and section of the
      form. Field_name should be unique within the group. Add two fields
      document_class and document_description.  For document_class generate a
      short name based on the document content like W4, I-9, Paystub.  For
      document_description generate a description about the document in less
      than 50 words. 

      Group the fields based on the section they are grouped in the form. Group
      should have attributeType as "group". If the group repeats and follows
      table format, update the attributeType as "list". Do not extract the
      values. Return the extracted data in JSON format. Format the extracted
      data using the below JSON format: Format the extracted groups and fields
      using the below JSON format:
  with_ground_truth:
    model_id: us.amazon.nova-pro-v1:0
    system_prompt: >-
      You are an expert in processing forms. Extracting data from images and
      documents. Use provided ground truth data as reference to optimize field
      extraction and ensure consistency with expected document structure and
      field definitions.
    temperature: 0
    top_p: 0
    max_tokens: 10000
    user_prompt: >-
      This image contains unstructured data. Analyze the data line by line using
      the provided ground truth as reference.                        
      <GROUND_TRUTH_REFERENCE> {ground_truth_json} </GROUND_TRUTH_REFERENCE>
      Ground truth reference JSON has the fields we are interested in extracting
      from the document/image. Use the ground truth to optimize field
      extraction. Match field names, data types, and groupings from the
      reference. Image may contain multiple pages, process all pages. Extract
      all field names including those without values. Do not change the group
      name and field name from ground truth in the extracted data json. Add
      field_description field for every field which will contain instruction to
      LLM to extract the field data from the image/document. Add data_type field
      for every field.  Add two fields document_class and document_description. 
      For document_class generate a short name based on the document content
      like W4, I-9, Paystub.  For document_description generate a description
      about the document in less than 50 words. If the group repeats and follows
      table format, update the attributeType as "list".                         
      Do not extract the values. Format the extracted data using the below JSON
      format: Format the extracted groups and fields using the below JSON
      format:
evaluation:
  enabled: true
  llm_method:
    top_p: 0
    max_tokens: 4096
    top_k: 5
    task_prompt: >-
      I need to evaluate attribute extraction for a document of class:
      {DOCUMENT_CLASS}.


      For the attribute named "{ATTRIBUTE_NAME}" described as
      "{ATTRIBUTE_DESCRIPTION}":

      - Expected value: {EXPECTED_VALUE}

      - Actual value: {ACTUAL_VALUE}


      Do these values match in meaning, taking into account formatting
      differences, word order, abbreviations, and semantic equivalence?

      Provide your assessment as a JSON with three fields:

      - "match": boolean (true if they match, false if not)

      - "score": number between 0 and 1 representing the confidence/similarity
      score

      - "reason": brief explanation of your decision


      Respond ONLY with the JSON and nothing else. Here's the exact format:

      {
        "match": true or false,
        "score": 0.0 to 1.0,
        "reason": "Your explanation here"
      }
    temperature: 0
    model: us.anthropic.claude-3-haiku-20240307-v1:0
    system_prompt: >-
      You are an evaluator that helps determine if the predicted and expected
      values match for document attribute extraction. You will consider the
      context and meaning rather than just exact string matching.
pricing: null
summary: null
criteria_types: null
request_bucket: null
request_history_prefix: null
criteria_bucket: null
output_bucket: null
textract_page_tracker: null
cost_report_bucket: null